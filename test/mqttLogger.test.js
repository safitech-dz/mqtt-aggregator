import { describe, it, expect } from "vitest";
import * as mqttLogger from "../mqtt/logger.js";

describe("mqttLogger", function () {
    it("1 is 1", function () {
        expect(1).toBe(1);
    });
});
